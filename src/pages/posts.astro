---
const response = await fetch("https://jsonplaceholder.typicode.com/posts"); // ? no es necesario ponerlo en una funcion con async
const posts = await response.json();
---

<h1>Mis publicaciones</h1>

{
  posts.map((post) => (
    <div>
      <h5>{post.userId}</h5>
      <h3 class="title">{post.title}</h3>
      <p class="content">{post.body}</p>
    </div>
  ))
}

<style>
  .title {
    color: skyblue;
  }
  .content {
    color: #333;
  }
</style>
